{
  "name": "vids-downloader",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/worker/**/*.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "@types/node": "^20",
    "eslint": "^9",
    "typescript": "^5",
    "wrangler": "^3.99.0"
  },
  "dependencies": {
    "ytdl-core": "^4.11.5"
  }
}
